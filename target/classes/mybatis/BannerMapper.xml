<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zerobase.fastlms.admin.mapper.BannerMapper">


    <sql id="selectListWhere">

        <if test="searchType != null and searchValue != null">
            <choose>
                <when test="searchType == 'bannerId'">
                    and banner_id like concat('%', #{searchValue}, '%')
                </when>
                <when test="searchType == 'bannerName'">
                    and banner_name like concat('%', #{searchValue}, '%')
                </when>
                <!-- <when test="searchType == 'phone'">
                    and phone like concat('%', #{searchValue}, '%')
                </when> -->
                <otherwise>
                    and
                    (
                    banner_id like concat('%', #{searchValue}, '%')
                    or
                    banner_name like concat('%', #{searchValue}, '%')
                    <!-- or
                    phone like concat('%', #{searchValue}, '%') -->
                    )
                </otherwise>
            </choose>
        </if>

    </sql>


    <select id="selectListCount" resultType="long">
        select count(*)
        from banner
        where 1 = 1
            <include refid="selectListWhere"/>
    </select>

    <select id="selectList" resultType="com.zerobase.fastlms.course.dto.CourseDto">
        select *
        from banner
        where 1 = 1
            <include refid="selectListWhere"/>
        order by reg_dt desc
        limit #{pageStart}, #{pageEnd}
    </select>

</mapper>
